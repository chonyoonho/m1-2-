# 보너스 과제 실행 결과

> `과제계획서.md` "5. 보너스 과제(선택)" 2개 항목에 대한 **실제 실행 결과**입니다.
> 아래 요청/응답은 로컬 및 실제 배포 서버(`https://three8demo-backend.onrender.com`)를 직접 호출하거나,
> 실제 배포 사이트(`https://frontend-blond-one-98.vercel.app`)를 헤드리스 브라우저(Playwright)로 조작해서 얻은 값입니다.

---

## 1) AI 도구 호출(Function Calling) + 멀티채널 연동(MCP Server)

### 무엇을 구현했나

시스템 프롬프트에는 전체 집계(기간/총합/평균/최대/최소/추세)만 주입되기 때문에, "2025년 월별로는 어땠어?",
"어떤 프로그램이 제일 인기 많아?" 같은 **요약만으로는 답할 수 없는 질문**을 받으면 GPT가 스스로 아래 3개
도구를 호출해 Firestore의 실제 데이터를 조회한 뒤 답하도록 구현했습니다.

| 도구 | 호출 시점 | 조회 내용 |
|---|---|---|
| `get_monthly_breakdown(year?)` | 특정 연도/전체 월별 수치, 최근 추이 질문 | 월별 총합/평균/건수 |
| `get_program_breakdown()` | 어떤 프로그램이 인기 있는지, 프로그램 비교 질문 | 프로그램별 총합/평균/건수 (내림차순) |
| `get_data_summary()` | 전체 요약을 다시 확인하고 싶을 때 | 기간/총합/평균/최대/최소/추세 |

**호출 흐름** (`backend/app/services/openai_service.py`의 `get_chat_reply`)

1. 시스템 프롬프트 + 대화 이력 + `tools` 스키마와 함께 GPT 호출
2. 응답의 `finish_reason`이 `tool_calls`이면, 각 도구를 로컬에서 실제로 실행(Firestore 조회)
3. 도구 실행 결과를 `role: tool` 메시지로 대화에 추가해 다시 GPT 호출
4. `tool_calls`가 더 이상 없을 때까지 2~3을 반복(최대 4회)한 뒤 최종 답변 반환

동일 기능을 [Model Context Protocol](https://modelcontextprotocol.io) 서버(`backend/mcp_server.py`)로도
노출해, Claude Desktop 같은 MCP 클라이언트에서 FastAPI를 거치지 않고 동일한 3개 도구·동일한 Firestore
데이터를 직접 조회할 수 있게 했습니다.

### 실제 검증 — Function Calling (배포 서버)

```
POST /api/chat   (https://three8demo-backend.onrender.com)
{"message": "어떤 프로그램이 참가자가 제일 많았어? 그리고 2023년 월별로는 어땠어?"}
```

GPT가 `get_program_breakdown()`과 `get_monthly_breakdown(year=2023)`을 스스로 호출한 뒤 반환한 실제 응답:

```json
{
  "conversation_id": "glbwjXGLsuYVTxntrrtg",
  "reply": "좋습니다. 조회 결과 기준으로 답변드립니다.\n\n1) 참가자가 가장 많은 프로그램\n- 프로그램명: 학교 연계 현장체험학습\n- 총합 참가자: 2,709명\n- 평균 참가자: 55.3명 (건당)\n- 건수(회수): 49회\n\n2) 2023년 월별 현황 (총합 / 평균 / 건수)\n- 2023-01: 63 / 31.5 / 2\n- 2023-02: 66 / 33.0 / 2\n- 2023-03: 242 / 60.5 / 4\n- 2023-04: 142 / 47.3 / 3\n- 2023-05: 199 / 49.8 / 4\n- 2023-06: 131 / 43.7 / 3\n- 2023-07: 63 / 31.5 / 2\n- 2023-08: 57 / 28.5 / 2\n- 2023-09: 177 / 44.2 / 4\n- 2023-10: 170 / 42.5 / 4\n- 2023-11: 140 / 46.7 / 3\n- 2023-12: 64 / 32.0 / 2\n\n요약 코멘트\n- 2023년 연간 총 참가자 수(월별 합계): 1,514명.\n- 최고월: 3월(242명), 다음으로 5월(199명), 9월(177명), 10월(170명).\n- 최저월: 8월(57명), 1·7월·12월도 낮음(각 63, 63, 64명).\n- 계절적 패턴: 봄(3~5월)과 가을(9~10월)에 참가자가 집중되는 경향이 있습니다."
}
```

같은 시점 `GET /api/data/statistics`가 반환한 원본 수치와 대조한 결과, 위 답변의 모든 숫자
(2,709 / 55.3명 / 49회, 2023년 각 월별 총합/평균/건수 전체)가 **정확히 일치**함을 확인했습니다 —
GPT가 실제로 도구를 호출해 받은 실데이터를 그대로 인용해 답했다는 뜻입니다.

로컬 환경에서도 별도 질문("이번 달 참가자가 어때? 그리고 가장 인기있는 프로그램은?")으로 동일하게
`get_program_breakdown` + `get_data_summary`가 함께 호출되는 것을 확인했습니다 (실제 화면은
`docs/screenshots/chat.png` 참고).

### 실제 검증 — MCP Server

```
$ python mcp_server.py   # stdio transport
```

실제 `mcp` Python SDK의 `ClientSession`으로 접속해서 확인한 결과:

```
initialize() → list_tools()
  ✓ get_data_summary
  ✓ get_monthly_breakdown
  ✓ get_program_breakdown

call_tool("get_program_breakdown", {})
  → {"programs": [{"program": "학교 연계 현장체험학습", "total": 2709.0, "average": 55.3, "count": 49}, ...]}
  → REST API(/api/data/statistics)와 완전히 동일한 값 반환

call_tool("get_monthly_breakdown", {"year": 2023})
  → {"monthly": [{"month": "2023-01", "total": 63.0, "average": 31.5, "count": 2}, ...]}
  → REST API와 완전히 동일한 2023년 월별 데이터 반환
```

Claude Desktop 등에서 사용하려면 `claude_desktop_config.json`에 아래와 같이 등록합니다.

```json
{
  "mcpServers": {
    "38demo-data": {
      "command": "python",
      "args": ["<repo>/backend/mcp_server.py"],
      "env": { "FIREBASE_SERVICE_ACCOUNT_JSON": "..." }
    }
  }
}
```

---

## 2) 인사이트·UX 고도화 (시각화 + 내보내기 + 선택 UI)

### 무엇을 구현했나

- **통계 확장**: `GET /api/data/statistics`가 기본 요약(`/api/data/summary`)에 **월별·프로그램별 세부 지표**를 추가로 제공 (`backend/app/services/analysis.py`의 `monthly_breakdown`/`program_breakdown`)
- **시각화**: 프론트엔드에 새 "통계" 탭을 만들어 Chart.js로 월별 참가자 수 추이 꺾은선 그래프 표시
- **데이터 내보내기**: "통계" 탭에서 전체 데이터를 CSV 또는 JSON으로 다운로드
- **다크 모드**: 헤더의 🌙/☀️ 버튼으로 전체 UI 다크 모드를 토글, 선택은 `localStorage`에 저장되고 차트 색상도 테마에 맞춰 함께 갱신

### 실제 검증 — API

```
GET /api/data/statistics   (배포 서버, 실제 응답)

summary.count: 245
monthly: 84개월치 (2019-01 ~ 2025-12)
by_program: [
  "학교 연계 현장체험학습" (총합 2709, 평균 55.3, 49건),
  "3.8민주아카데미 오늘을 묻다" (총합 2240, 평균 26.7, 84건),
  "보드게임으로 만나는 민주주의" (총합 1708, 평균 20.3, 84건),
  "3.8민주의거 기념식 연계 교육" (총합 598, 평균 85.4, 7건),
  "청소년 백일장" (총합 306, 평균 43.7, 7건),
  "청소년 사생대회" (총합 286, 평균 40.9, 7건),
  "시민 해설사 양성과정" (총합 282, 평균 40.3, 7건)
]
```

### 실제 검증 — 실제 배포 사이트를 브라우저로 조작 (Playwright)

통계 탭 클릭 → 차트·표 렌더링 → 다크 모드 토글(차트 색상도 함께 재조정) → CSV 다운로드 → JSON 다운로드,
전체 과정에서 **콘솔 에러 0건**을 확인했습니다.

```
== 통계 탭: 프로그램별 표 ==
program rows: 7
first row: 학교 연계 현장체험학습   2709   55.3   49

== 다크 모드 토글 ==
data-theme now: dark

== CSV 다운로드 ==
csv filename: 38demo-data.csv

== JSON 다운로드 ==
json filename: 38demo-data.json

== 콘솔 에러 ==
[]
```

**통계 탭 — 라이트/다크 모드 실제 캡처**

| 라이트 모드 | 다크 모드 |
|---|---|
| ![통계 화면 라이트](docs/screenshots/stats-light.png) | ![통계 화면 다크](docs/screenshots/stats-dark.png) |
